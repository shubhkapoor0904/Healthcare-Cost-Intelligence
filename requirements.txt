# No runtime ML dependencies — embeddings are pre-computed.
# The server runs on Python's standard library only.
#
# To regenerate embeddings (optional, one-time):
#   pip install sentence-transformers
#   python seed_db.py
